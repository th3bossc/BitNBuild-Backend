from django.urls import path
# from .views import ProfileInfo, WatchList, History

app_name = 'users'
urlpatterns = [
    # path('profile/', ProfileInfo.as_view(), name='profile'),
    # path('watchlist/', WatchList.as_view(), name='watchlist'),
    # path('history/', History.as_view(), name='history'),
]